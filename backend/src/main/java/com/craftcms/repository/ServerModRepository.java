package com.craftcms.repository;

import com.craftcms.model.ServerMod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ServerModRepository extends JpaRepository<ServerMod, Long> {
}
